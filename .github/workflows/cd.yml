name: CD

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
    paths:
      - .github/workflows/*
      - src/**
      - '*.md'

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.experimental }}
    strategy:
      fail-fast: true
      matrix:
        experimental: [true]
        os: [ubuntu-latest, windows-latest, macos-latest]
        arch: [amd64, arm64]

    name: 🚀 CD on ${{ matrix.os }} ${{ matrix.arch }}

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v5

      - name: 🔧 Setup Vlang
        uses: siguici/setup-v@v0

      - name: 📦 Build for Unix
        if: runner.os != 'Windows'
        run: |
          mkdir -p bin
          OUTPUT="bin/sg_${RUNNER_OS}_${{ matrix.arch }}"
          echo "Building $OUTPUT..."
          v -prod -arch ${{ matrix.arch }} . -o $OUTPUT
          zip -j "${OUTPUT}.zip" "$OUTPUT"
          sha256sum "${OUTPUT}.zip" > "${OUTPUT}.zip.sha256"

      - name: 📦 Build for Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path bin
          $output = "bin\sg_${env:RUNNER_OS}_${{ matrix.arch }}.exe"
          Write-Host "Building $output..."
          v -prod -arch ${{ matrix.arch }} . -o $output
          Compress-Archive -Path $output -DestinationPath "$output.zip"
          Get-FileHash "$output.zip" -Algorithm SHA256 | ForEach-Object { $_.Hash } | Set-Content "$output.zip.sha256"

      - name: 📂 Upload binaries
        id: gh_release
        uses: softprops/action-gh-release@v2
        if: github.ref_type == 'tag'
        with:
          files: |
            bin/*.zip
            bin/*.zip.sha256
            *.md
